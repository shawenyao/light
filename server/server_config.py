# light server API endpoint
server_host = "127.0.0.1"
server_port = 2023
api = f"http://{server_host}:{server_port}/light/control"

# kasa plug ip
kasa_host = "xxx.xxx.x.xxx"

# duration of lighting (in seconds)
duration = 120

# interval between trigger
trigger_interval = 0.1
