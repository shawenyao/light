# api endpoint
host = "xxx.xxx.x.xx"
port = 2023
api = f"http://{host}:{port}/picow/pir"

# wifi
ssid = "ssid"
password = "password"

# motion sensor sampling interval
# during both on and off states of the siginal
interval_on = 10
interval_off = 0.25

# GPIO pin number for the PIR sensor
pir_pin = 0
